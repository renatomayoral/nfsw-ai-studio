{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,re; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); exit(0 if re.search(r'packages/db/src/(schema|creators)\\.ts$', f) else 1)\" && cd /media/renato/dados/dev/creators-link/packages/db && pnpm drizzle-kit generate && pnpm drizzle-kit migrate",
            "timeout": 60,
            "statusMessage": "Rodando drizzle generate + migrate..."
          }
        ]
      }
    ]
  }
}
